<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- VPN・フォアグラウンドサービス・使用状況アクセスの権限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- オーバーレイでアプリの上に全画面表示する（フローティング表示権限）-->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Android 13以降で通知を表示するために必要 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 再起動後にサービスを自動復帰させるために必要 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- バッテリー最適化の除外をリクエストするために必要 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- アプリブロック: フォアグラウンドアプリの検出に必要（設定から許可が必要）-->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"
        xmlns:tools="http://schemas.android.com/tools" />

    <application
        android:label="Toxov"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ブロッカー画面: singleTaskで常に1枚だけ表示、戻るボタンでホームへ -->
        <activity
            android:name=".BlockerActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity="com.toxov.blocker"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:showOnLockScreen="true" />

        <!-- VpnService：BIND_VPN_SERVICE権限で保護する -->
        <service
            android:name=".ToxovVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
            <!-- specialUseタイプには用途の説明が必要 -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="DNS filtering for internet detox" />
        </service>

        <!-- アプリブロッカーサービス：使用状況アクセスでフォアグラウンドアプリを監視する -->
        <service
            android:name=".AppBlockerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="App usage monitoring for internet detox" />
        </service>

        <!-- サイトブロック：ブラウザのURLをアクセシビリティAPIで検知してオーバーレイ表示する -->
        <service
            android:name=".ToxovAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/toxov_accessibility_config" />
        </service>

        <!-- 緊急解除24時間タイムアウトで自動ブロック再開するAlarmManagerレシーバー -->
        <receiver
            android:name=".EmergencyResumeReceiver"
            android:exported="false" />

        <!-- 再起動後にAppBlockerServiceを自動復帰させるレシーバー -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ホーム画面ウィジェット -->
        <receiver
            android:name=".ToxovWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/toxov_widget_info"/>
        </receiver>

        <!-- flutter_local_notifications: スケジュール通知の発火レシーバー -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <!-- flutter_local_notifications: 再起動後にスケジュール通知を復元するレシーバー -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- ランチャーから起動できるアプリ一覧を取得するために必要（Android 11+）-->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
        <!-- url_launcher: ブラウザでhttpsページを開くために必要（Android 11+）-->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
